#include <bitmap-font-renderer.h>

#include <stb_image.h>

BitmapFontRenderer::BitmapFontRenderer() {
	// Load a bitmap with glyphs into the OpenGL context
	
}

BitmapFontRenderer::~BitmapFontRenderer() {
	// Remove the loaded bitmap from the OpenGL context
}

void BitmapFontRenderer:draw(const std::string& text, glm::vec2 position, glm::vec2 resolution, glm::vec3 color) {
	
}
